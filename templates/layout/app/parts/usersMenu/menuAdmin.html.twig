<nav class="mt-2">
	<ul class="nav nav-pills nav-sidebar flex-column" data-widget="treeview" role="menu" data-accordion="false">
		<li class="nav-item">
			<a href="{{ path('app_index') }}" class="nav-link {{ menuItemIsActive(app.request.pathInfo, path('app_index')) }}">
				<i class="nav-icon fas fa-th"></i>
				<p>Strona główna</p>
			</a>
		</li>
		<li class="nav-item has-treeview {{ menuIsOpen(app.request.pathInfo, ['user']) }}">
			<a href="#" class="nav-link {{ aLinkIsActive(app.request.pathInfo, ['user']) }}">
				<i class="nav-icon fas fa-user"></i>
				<p>Użytkownicy
					<i class="right fas fa-angle-left"></i>
				</p>
			</a>
			<ul class="nav nav-treeview">
				<li class="nav-item">
					<a href="{{ path('user_index') }}" class="nav-link {{ menuItemIsActive(app.request.pathInfo, path('user_index')) }}">
						<i class="nav-icon fas fa-user"></i>
						<p>Użytkownicy</p>
					</a>
				</li>
				<li class="nav-item">
					<a href="{{ url('user_add') }}" class="nav-link {{ menuItemIsActive(app.request.pathInfo, path('user_add')) }}">
						<i class="fas fa-plus-circle nav-icon"></i>
						<p>Dodaj użytkownika</p>
					</a>
				</li>
			</ul>
		</li>
	</ul>
</nav>
